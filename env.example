# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# App URL (for email confirmation links)
# In production, set this to your actual domain (e.g., https://yourdomain.com)
# In development, leave as localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

NODE_ENV=production

# Resend Email API Key (Optional but recommended)
# Get your free API key from: https://resend.com/signup
# Free tier: 3,000 emails/month, 100 emails/day
RESEND_API_KEY=your_resend_api_key

# Resend Configuration
# IMPORTANT: Use the verified subdomain, not the root domain
# Based on your DNS records, use: waitlist.hyperionkit.xyz (verified subdomain)
# NOT: hyperionkit.xyz (root domain - not verified)
RESEND_FROM_EMAIL=team@waitlist.hyperionkit.xyz
RESEND_TEST_EMAIL=hyperkitdev@gmail.com
